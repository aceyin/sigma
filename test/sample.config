%% Sample config: {key:atom(), value:any()}
{sample_conf, 100}.

%% Map config: {key:atom(), value:map()}
{ map_conf, #{
  id => 1,
  name => "sigma_server",
  cookie => [1, 2, 3]
}}.

%% List config: {key:atom(), value:list()}
{list_conf, [1, "string", #{key => value}]}.
